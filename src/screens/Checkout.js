import { Fragment } from "react"
import { Text } from "react-native"

const Checkout = (props) => {
    return (
        <Fragment>
            <Text>
                Checkout
            </Text>
        </Fragment>
    )
}

export default Checkout